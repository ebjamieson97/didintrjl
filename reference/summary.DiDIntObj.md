# Summary method for `DiDIntObj`

Summary method for `DiDIntObj`

## Usage

``` r
# S3 method for class 'DiDIntObj'
summary(object, level = c("agg", "sub"), ...)
```

## Arguments

- object:

  A `DiDIntObj` object

- level:

  Specify either `"agg"` or `"sub"` to view the aggregate or
  sub-aggregate results.

- ...:

  other arguments
